## React BoxyChat Boilerplate

It is a basic React.js project boilerplate, it uses JSX, React Router, Browserify and SASS.

### Usage
- `npm install`
- `npm start`

or
- `gulp serve`

while if you just need to build `gulp`
